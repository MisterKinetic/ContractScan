# ContractScan AI Worker — Python Dependencies
# Setup: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# PDF parsing (Stage 2)
PyMuPDF==1.24.5

# NER / PII redaction (Stage 1)
spacy==3.7.5
# After pip install, also run: python -m spacy download en_core_web_lg

# Embeddings (Stage 4)
sentence-transformers==3.1.1

# Database
psycopg2-binary==2.9.9    # PostgreSQL driver
pgvector==0.3.2           # PGVector Python client
sqlalchemy==2.0.31        # ORM

# Redis (job queue consumer)
redis==5.0.8
rq==1.16.2                # Redis Queue — simpler than Celery for this use case

# HTTP / API
fastapi==0.112.0
uvicorn==0.30.6
httpx==0.27.0             # async HTTP for Claude API calls

# Anthropic SDK
anthropic==0.34.0

# Utilities
python-dotenv==1.0.1
pydantic==2.8.2
loguru==0.7.2             # better logging than stdlib
